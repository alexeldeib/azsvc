name: "install bootstrapper"
runs:
    using: 'docker'
    image: 'Dockerfile'
    args:
    - -f
    - ${{ inputs.file }}
inputs:
    file:
        description: "file containing cluster configuration to bootstrap"
        required: true
